# Vite (browser) — same host ports as parent ../docker-compose.yml defaults
VITE_CONVEX_URL=http://127.0.0.1:3210
VITE_CONVEX_SITE_URL=http://127.0.0.1:3211

# Convex CLI uses ../.env (Docker + generate-admin-key). From parent: docker compose --profile tools run --rm generate-admin-key
